services:
  - type: web
    name: community-project-api
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt && python backend/manage.py migrate --no-input && python backend/manage.py collectstatic --no-input
    startCommand: gunicorn backend.config.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: community-project-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: True
      - key: API_BASE
        value: "https://community-project-abtu.onrender.com/api"
    autoDeploy: true

databases:
  - name: community-project-db
    plan: free